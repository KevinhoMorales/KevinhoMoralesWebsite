/** Shared horizontal padding for homepage body sections. */
export const SECTION_PADDING_X =
  'px-4 sm:px-6 md:px-8 lg:px-12 xl:px-24'

/** Tighter vertical rhythm between stacked sections. */
export const SECTION_PADDING_Y = 'py-2 sm:py-3 md:py-4'

export const SECTION_PADDING = `${SECTION_PADDING_X} ${SECTION_PADDING_Y}`

/** Slightly roomier blocks (book, achievements). */
export const SECTION_PADDING_Y_LOOSE = 'py-3 sm:py-5 md:py-7'

/** Connect block on the home page. */
export const SECTION_PADDING_Y_CONNECT_HOME = 'py-4 sm:py-10 md:py-12'
